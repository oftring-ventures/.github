name: Codex review

on:
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review]

permissions:
  contents: read
  actions: read

jobs:
  codex_review:
    if: ${{ !github.event.pull_request.draft }}
    uses: oftring-ventures/.github/.github/workflows/codex-pr-review.yml@04eeb44565d6efaa7e8086a072d87583a75a9f61
    secrets: inherit
