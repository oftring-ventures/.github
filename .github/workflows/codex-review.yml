name: Codex review

on:
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review]

permissions:
  contents: read

jobs:
  codex_review:
    if: ${{ !github.event.pull_request.draft }}
    uses: oftring-ventures/.github/.github/workflows/codex-pr-review.yml@37e790d29a670e100f7e728e7c3825f536bd10e9
    secrets: inherit
